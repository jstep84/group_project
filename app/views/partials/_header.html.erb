<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Essential</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
          <%= form_tag '/shelter', method: :get do |f| %>
            <%= hidden_field_tag 'long3'%>
            <%= hidden_field_tag 'lat3'%>
            <button type="submit" class="btn btn-link">
              Shelter
            </button>
          <% end %>
        </li>

        <li class="active">
          <%= form_tag '/meal', method: :get do |f| %>
            <%= hidden_field_tag 'long4'%>
            <%= hidden_field_tag 'lat4'%>
            <button type="submit" class="btn btn-link">
              Meals
            </button>
          <% end %>
        </li>

        <li><a href="/static_pages/resources">Other Resources</a></li>
        <li><a href="/static_pages/about">About</a></li>
        <li><a href="/profile/create">Add a Profile</a></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if @current_user %>
            <li class="signMsg"><%= "Signed in as: " + @current_user.username %></li>
            <li> <%= link_to "Logout", "/logout" %></li>
        <% else %>
            <li><%= link_to "Login", "/login" %></li> 
            <li> <%= link_to "Signup", "/signup" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>